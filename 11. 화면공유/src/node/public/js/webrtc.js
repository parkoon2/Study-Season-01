(function( global ) {

    function getMedia( streamId ) {
        hasUserMedia();
    }

    function hasUserMedia() {

        navigator.getUserMedia
    }

    global.webrtc = {
        getMedia,
    }
})( this );